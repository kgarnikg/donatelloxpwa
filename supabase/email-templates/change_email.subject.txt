{{ $l := or .Data.locale "en" }}{{ if eq $l "ru" }}Подтверди новый email — DonatelleX{{ else if eq $l "es" }}Confirma tu nuevo email — DonatelleX{{ else if eq $l "de" }}Bestätige deine neue E-Mail — DonatelleX{{ else if eq $l "it" }}Conferma la nuova email — DonatelleX{{ else if eq $l "uk" }}Підтверди новий email — DonatelleX{{ else if eq $l "hy" }}Հաստատիր նոր email-ը — DonatelleX{{ else if eq $l "ar" }}أكّد بريدك الجديد — DonatelleX{{ else if eq $l "hi" }}अपना नया ईमेल कन्फ़र्म करें — DonatelleX{{ else if eq $l "pa" }}ਆਪਣੀ ਨਵੀਂ ਈਮੇਲ ਪੁਸ਼ਟੀ ਕਰੋ — DonatelleX{{ else }}Confirm your new email — DonatelleX{{ end }}
