{{ $l := or .Data.locale "en" }}{{ if eq $l "ru" }}Подтверди email — DonatelleX{{ else if eq $l "es" }}Confirma tu email — DonatelleX{{ else if eq $l "de" }}Bestätige deine E-Mail — DonatelleX{{ else if eq $l "it" }}Conferma la tua email — DonatelleX{{ else if eq $l "uk" }}Підтверди email — DonatelleX{{ else if eq $l "hy" }}Հաստատիր email-ը — DonatelleX{{ else if eq $l "ar" }}أكّد بريدك الإلكتروني — DonatelleX{{ else if eq $l "hi" }}अपना ईमेल कन्फ़र्म करें — DonatelleX{{ else if eq $l "pa" }}ਆਪਣੀ ਈਮੇਲ ਪੁਸ਼ਟੀ ਕਰੋ — DonatelleX{{ else }}Confirm your email — DonatelleX{{ end }}
