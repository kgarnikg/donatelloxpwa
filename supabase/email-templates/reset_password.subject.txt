{{ $l := or .Data.locale "en" }}{{ if eq $l "ru" }}Восстановление пароля — DonatelleX{{ else if eq $l "es" }}Restablece tu contraseña — DonatelleX{{ else if eq $l "de" }}Passwort zurücksetzen — DonatelleX{{ else if eq $l "it" }}Reimposta la password — DonatelleX{{ else if eq $l "uk" }}Відновлення пароля — DonatelleX{{ else if eq $l "hy" }}Գաղտնաբառի վերականգնում — DonatelleX{{ else if eq $l "ar" }}إعادة تعيين كلمة المرور — DonatelleX{{ else if eq $l "hi" }}पासवर्ड रीसेट करें — DonatelleX{{ else if eq $l "pa" }}ਪਾਸਵਰਡ ਰੀਸੈੱਟ ਕਰੋ — DonatelleX{{ else }}Reset your password — DonatelleX{{ end }}
